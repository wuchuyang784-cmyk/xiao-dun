# 2026-07-30

## 重写项目的部署启动 README
- 通读 `package.json` / `scripts/start-web.mjs` / `scripts/web-runtime.mjs` / `src/index.js` / `src/config.js` / `src/api.js` / `src/paths.js` / `scripts/start-lan.ps1`，梳理出启动方式。
- 关键信息：Node>=22、ESM、无构建步骤；`npm start` 走 Web 模式（自动开浏览器，端口 3721）；`XIAODUN_PORT`/`XIAODUN_HOST`/`XIAODUN_NO_OPEN`/`XIAODUN_WEB_ONLY`/`XIAODUN_ALLOW_LAN`/`XIAODUN_USER_DIR` 环境变量；`.env` 经 `--env-file-if-exists` 加载 LLM 密钥自动激活；原生模块 better-sqlite3/sherpa-onnx-node；首次本地嵌入下载约330MB ONNX；数据落在仓库根（部署建议用 XIAODUN_USER_DIR 外置+备份）。
- 注意仓库内 `config.json` 明文含 TTS/ClawBot 凭据，README 已提醒勿提交。
- 已重写 `E:\xiao-dun\README.md` 为完整部署/启动指南（含 pm2/systemd/Nginx/Docker 可选方案、故障排查）。
